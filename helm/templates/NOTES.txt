The gnis stack has been deployed as release {{ .Release.Name }} in namespace {{ .Release.Namespace }}.

{{- if .Values.ingress.enabled }}

Once the pod is ready (the readiness probes allow up to 90 minutes for GraphDB
and PostGIS to come up), the webapp will be available at:

  http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.host }}/
{{- else }}

The ingress is disabled; reach the webapp through the service:

  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "gnis.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}

Watch the rollout with:

  kubectl -n {{ .Release.Namespace }} rollout status deployment/{{ include "gnis.fullname" . }}

To trigger a graph update manually (re-runs the triplifier):

  kubectl -n {{ .Release.Namespace }} rollout restart deployment/{{ include "gnis.fullname" . }}
